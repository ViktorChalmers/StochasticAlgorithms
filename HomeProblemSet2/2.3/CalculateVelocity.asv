CalculateVelocity()
function vel = CalculateVelocity(alpha,gear,brakePressure,brakeTemperature,mass)
Fg = M*g*sin(alpha)
if brakeTemperature < brakeTemperatureMax-100
    Fb = M*g/(20)*brakePressure
else
    Fb = M*g/20*brakePressure*exp(-(brakeTemperature-(brakeTemperatureMax-100))/100)
end    
Feb = []
a = (Fg-Fb-Feb)/M



end